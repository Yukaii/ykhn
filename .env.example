# Browser-facing override. Leave unset in local dev to use Vite's same-origin
# /auth-proxy development proxy.
# VITE_HN_AUTH_PROXY_URL=https://hn-api.yukai.dev

# Vite dev-server proxy target for /auth-proxy.
HN_AUTH_PROXY_TARGET=https://hn-api.yukai.dev
